var attendanceApp = attendanceApp || {};

attendanceApp.generateStudents = function(){
	if(!localStorage.attendance){
		function getRandom() {
            return (Math.random() >= 0.5);
        }
		
		
	}
}